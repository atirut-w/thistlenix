#ifndef UTILS_H
#define UTILS_H

// Prints a message to the console.
void print(const char *message);
// Logs a message to the console.
void log(const char *message);
// Get the current uptime in ticks.
int get_uptime();

#endif
